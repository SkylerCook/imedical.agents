# 项目规则

保存项目特有的稳定约束；高频保护引用 `../../AGENTS.md`，上下文模式及代码范围引用 `../config/project_context_profile.md`，不重复维护配置值。

## 开发约定

TODO: 记录已验证且影响后续任务的约定、公共资源影响面或专项规则入口；删除不适用占位，不据少量文件推断完整架构。

## 维护分工（按需保留）

TODO: 有实际维护分工时，明确保护对象、职责判定及授权例外；不把示例清单当作全部边界，也不按目录扩大禁令。

## 验证

按实际改动及风险检查受影响行为；保留项目必需门禁，区别本地检查与真实环境验收。长期规则放本文件，配置归 profile，历史定位线索归 memory。
