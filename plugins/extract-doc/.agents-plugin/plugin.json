{
  "name": "extract-doc",
  "version": "0.2.0",
  "displayName": "Extract Doc Plugin",
  "description": "Reusable document ingestion workflow for converting PDF, DOC, DOCX, XLS, and XLSX files into Markdown, interface-oriented fields, and optional domain-neutral document structure artifacts.",
  "entry": "AGENTS.md",
  "rules": "rules/",
  "skills": "skills/",
  "scripts": "scripts/",
  "initSkill": "extract-doc-ingest",
  "requiresProjectFiles": [
    "AGENTS.md"
  ],
  "defaultPrompt": [
    "Check parser availability for this document",
    "Extract this document into Markdown and structured JSON artifacts"
  ]
}
