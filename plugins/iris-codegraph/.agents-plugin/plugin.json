{
  "name": "iris-codegraph",
  "version": "0.1.3",
  "displayName": "IRIS CodeGraph Plugin",
  "description": "Build and query a local SQLite graph for IRIS/ObjectScript classes, methods, properties, parameters, inheritance, and static ##class calls.",
  "entry": "AGENTS.md",
  "rules": "rules/",
  "skills": "skills/",
  "scripts": "scripts/",
  "references": "schema.md",
  "initSkill": "iris-codegraph",
  "dependencies": [
    "coding-iris-plugin"
  ],
  "dependencyVersions": {
    "coding-iris-plugin": {
      "minVersion": "0.3.0",
      "maxVersionExclusive": "0.7.0"
    }
  },
  "requiresProjectFiles": [
    "AGENTS.md",
    ".mcp.json"
  ],
  "defaultPrompt": [
    "Build an IRIS ObjectScript graph from exported class cache",
    "Query IRISGraph for a class or method",
    "Find ObjectScript callers and callees in the IRIS graph",
    "Estimate impact for changing an ObjectScript class"
  ]
}
