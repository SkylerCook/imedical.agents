Node.js ESM/CommonJS hybrid scripts using built-in `child_process`, `fs`, `path`; `iconv-lite` for UTF-8 ↔ GB2312 conversion; direct `https` module calls to GitLab REST API (with `rejectUnauthorized: false` for internal IP hosts); external `psftp` (PuTTY) executable for SFTP transfers.