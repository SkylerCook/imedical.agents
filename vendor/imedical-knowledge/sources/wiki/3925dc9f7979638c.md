InterSystems IRIS ObjectScript backend, jQuery + HISUI (EasyUI-based) frontend with CSP pages, Node.js scripts (ESM/CommonJS) for Git/SFTP orchestration, and an MCP toolchain for IRIS compile/upload operations; all products share a single backend and frontend submodule plus shared `public-mc/` resources.